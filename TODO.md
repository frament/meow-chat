# TODO

## High Priority
- [ ] End-to-end federation test between two instances
- [ ] `HandleForwardPostImages` endpoint if needed for image proxy
- [ ] `new_messages_encrypted`/`new_messages_iv` columns for `group_messages`

## Medium Priority
- [ ] Multi-server pub/sub (Redis/NATS) for horizontal scaling
- [ ] Chat list virtualization for large groups (`@angular/cdk`)

## Low Priority
- [ ] PWA install prompt — track install dismissed state (don't re-show after dismiss)
- [ ] Avatar upload progress indicator
- [ ] Backup upload progress indicator
